const test=require('node:test');const assert=require('node:assert/strict');const telemetry=require('../retrievalTelemetryStore');
test('normalizes compact retrieval scene telemetry',()=>{const row=telemetry.normalizeScene({scene_index:'2',visual_mode:' exact_real ',must_show:'woodpecker pecking a tree',requested_queries:['woodpecker pecking tree','woodpecker close up'],source_priority:['wikimedia','pexels_video'],queries_tried:['woodpecker pecking tree'],candidate_count:'17',candidate_source_counts:{pexels_video:10,wikimedia:7,bad:'x'},candidate_type_counts:{video:10,image:7},scored_count:3,vision_calls:3,cache_hits:1,search_rounds:2,selected_query:'woodpecker pecking tree',selected_source:'pexels_video',score:71,relevance:78,local_similarity:69,top_local_similarity:82,threshold:72,degraded:true,quality_gate_passed:false,fallback_reason:'below_quality_threshold',intentional_template:false});assert.equal(row.scene_index,2);assert.equal(row.visual_mode,'exact_real');assert.equal(row.candidate_count,17);assert.deepEqual(row.candidate_source_counts,{pexels_video:10,wikimedia:7});assert.deepEqual(row.candidate_type_counts,{video:10,image:7});assert.equal(row.top_local_similarity,82);assert.equal(row.degraded,true);assert.equal(row.quality_gate_passed,false);});
test('caps telemetry payload size',()=>{const out=telemetry.normalizeTelemetry({video_id:'abc123',topic:'test',retrieval_telemetry:Array.from({length:20},(_,i)=>({scene_index:i,requested_queries:Array.from({length:20},(_,q)=>`query-${q}`)}))});assert.equal(out.video_id,'abc123');assert.equal(out.scenes.length,12);assert.equal(out.scenes[0].requested_queries.length,6);});
